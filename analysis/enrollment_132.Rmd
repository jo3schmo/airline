---
title: "enrollment"
output: html_notebook
---

##loading packages
```{r}
library(tidyverse)
library(here)
library(janitor)
library(readr)
library(ggplot2)
library(forecast)
library(fpp2)
library(TTR)
library(dplyr)
```

##load in all data
```{r}
thisfile=here("data_raw","CU_ACAD_DEPT_WHEN_OFF_ENRL_22740.csv")

df_m = read_csv(thisfile) %>% clean_names()
```

##transform data
```{r}
target <- c("131","132")
df_1 <- df_m %>% filter(catalog == target, section <10) %>% select(catalog, term, term2, tot_enrl, section, title)
```

```{r}
df_2 <- df_1 %>% 

```


